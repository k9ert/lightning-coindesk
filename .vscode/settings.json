{
    "python.pythonPath": "${workspaceFolder}/deskenv/bin/python",
    "python.linting.pylintEnabled": false,
    "files.exclude": {
        "**/*.pyc": true,
        //"**/*.pyc": { "when": "$(basename).py"}        
        
        
    }
}